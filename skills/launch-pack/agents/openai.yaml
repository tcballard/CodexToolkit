interface:
  display_name: "Launch Pack"
  short_description: "Turn a verified release into one coherent launch package"
  default_prompt: "Use $launch-pack to plan, assemble, and validate an evidence-backed launch package for this release."
policy:
  allow_implicit_invocation: true
